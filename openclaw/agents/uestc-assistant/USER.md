# USER — 用户偏好种子

> 本文件是主 Agent 的**只读用户偏好格式说明**，运行时不得追加写入。
> 长期用户偏好请写入：`workspace/_state/user-preferences.md`。
> 当前会话状态请写入：`workspace/{session_id}/state/session.md`。

推荐格式：

```text
- [YYYY-MM-DD] 偏好内容；来源：用户明确表达；适用范围：全局|当前课程|当前任务；是否可长期保留：yes|no
```

当前无预置用户偏好。
